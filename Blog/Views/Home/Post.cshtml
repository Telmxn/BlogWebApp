@model Post

@{ 
    ViewBag.Title = Model.Title;
    ViewBag.Desctiption = Model.Description;
    ViewBag.Keywords = $"{Model.Tags?.Replace(",", " ")} {Model.Category}";
}
@*<h2>@Model.Title</h2>

    @if (!String.IsNullOrEmpty(Model.Image))
    {
        var image_path = $"/Image/{Model.Image}";
        <img src="@image_path" style="width:500px;" />
    }

    <p>@Model.Body</p>*@

<div class="container">
    <div class="post no-shadow" style="margin: 0;">
        @if (!string.IsNullOrEmpty(Model.Image))
        {
            var image_path = $"/Image/{Model.Image}";
            <img src="@image_path" />
            <span class="title">@Model.Title</span>
        }
    </div>
    <div class="post-body">
        @Html.Raw(Model.Body)
    </div>
</div>